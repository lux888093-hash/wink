<view class="shell">
  <view wx:if="{{ready}}" class="page-scroll melody-page">
    <image class="melody-bg" src="/assets/images/melody-phone-bg.jpg" mode="aspectFill"></image>
    <view class="melody-bg-shadow"></view>
    <view class="melody-bg-overlay"></view>

    <view class="melody-topbar">
      <button class="topbar-button">
        <image class="topbar-icon" src="/assets/icons/menu.svg" mode="aspectFit"></image>
      </button>
      <text class="topbar-title">THE CURATOR</text>
      <button class="topbar-button">
        <image class="topbar-icon" src="/assets/icons/search.svg" mode="aspectFit"></image>
      </button>
    </view>

    <view class="melody-stage">
      <view class="cover-shell">
        <image class="cover-image" src="/assets/images/melody-phone-cover.jpg" mode="aspectFill"></image>
        <view class="cover-core"></view>
      </view>

      <text class="eyebrow melody-kicker">AMBIENT PAIRING</text>
      <text class="display-title melody-title">{{currentTrack.title}}</text>
      <text class="muted-copy melody-description line-clamp-2">{{currentTrack.description}}</text>

      <view class="progress-wrap">
        <view class="progress-rail">
          <view class="progress-fill" style="width: {{progress}}%;"></view>
        </view>
        <view class="progress-labels">
          <text>{{currentTimeLabel}}</text>
          <text>{{durationLabel}}</text>
        </view>
      </view>

      <view class="control-row">
        <button class="control-side" bindtap="playPrev">
          <image class="control-side-icon" src="/assets/icons/skip_previous.svg" mode="aspectFit"></image>
        </button>
        <button class="control-main" bindtap="togglePlayback">
          <image
            wx:if="{{isPlaying}}"
            class="control-main-icon"
            src="/assets/icons/pause.svg"
            mode="aspectFit"
          ></image>
          <image
            wx:else
            class="control-main-icon play-icon"
            src="/assets/icons/play_arrow.svg"
            mode="aspectFit"
          ></image>
        </button>
        <button class="control-side" bindtap="playNext">
          <image class="control-side-icon" src="/assets/icons/skip_next.svg" mode="aspectFit"></image>
        </button>
      </view>

      <view wx:if="{{gateMessage}}" class="gate-inline">
        <text class="micro-copy gate-copy">{{gateMessage}}</text>
        <view class="gate-links">
          <button wx:if="{{entryScope === 'exclusive'}}" class="gate-link" bindtap="openDetail">查看酒款</button>
          <button wx:else class="gate-link" bindtap="openMember">会员权益</button>
          <text class="gate-sep">/</text>
          <button class="gate-link" bindtap="unlockCurrentTrack">单曲解锁</button>
        </view>
      </view>

      <view class="selector-wrap">
        <text class="selector-label">SELECT WINE MOOD</text>
        <view class="selector-row">
          <button
            class="track-chip {{currentTrackIndex === index ? 'is-active' : ''}}"
            wx:for="{{tracks}}"
            wx:key="id"
            wx:for-index="index"
            data-index="{{index}}"
            bindtap="selectTrack"
          >
            {{item.title}}
          </button>
        </view>
      </view>
    </view>

    <view class="tabbar-gap" wx:if="{{!trackId}}"></view>
    <editorial-tabbar wx:if="{{!trackId}}" active="melody"></editorial-tabbar>
  </view>

  <view wx:else class="page-scroll empty-state">
    <text class="empty-title">{{errorTitle}}</text>
    <text class="empty-subtitle">{{errorMessage}}</text>
    <navigator wx:if="{{entryScope === 'exclusive'}}" class="ghost-button" url="/pages/redeem/index">返回扫码页</navigator>
    <navigator wx:elif="{{entryScope === 'member'}}" class="ghost-button" url="/pages/member/index">返回会员中心</navigator>
    <navigator wx:else class="ghost-button" url="/pages/home/index">返回首页</navigator>
  </view>
</view>
