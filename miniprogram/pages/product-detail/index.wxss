.product-page {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  padding-bottom: 228rpx;
}

.product-gallery {
  position: relative;
  overflow: hidden;
  border-radius: 34rpx;
  background:
    radial-gradient(circle at 50% 22%, rgba(231, 200, 150, 0.16), rgba(231, 200, 150, 0)),
    linear-gradient(180deg, rgba(32, 24, 22, 0.98), rgba(17, 13, 12, 0.96));
}

.product-swiper {
  height: 560rpx;
}

.product-swiper-item {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 560rpx;
  padding: 72rpx 42rpx 84rpx;
}

.product-swiper-image {
  width: 100%;
  height: 100%;
}

.product-gallery-head {
  position: absolute;
  top: 18rpx;
  left: 18rpx;
  right: 18rpx;
  z-index: 2;
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12rpx;
}

.product-chip-row {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
}

.product-chip {
  display: inline-flex;
  align-items: center;
  min-height: 42rpx;
  padding: 0 16rpx;
  border-radius: 999rpx;
  background: rgba(244, 235, 225, 0.08);
  color: var(--ink);
  font-size: 16rpx;
  line-height: 1;
}

.product-chip-muted {
  color: var(--ink-soft);
  background: rgba(244, 235, 225, 0.05);
}

.product-gallery-index {
  flex-shrink: 0;
  min-width: 92rpx;
  min-height: 42rpx;
  padding: 0 16rpx;
  border-radius: 999rpx;
  background: rgba(8, 6, 5, 0.38);
  color: var(--ink-soft);
  font-size: 16rpx;
  line-height: 42rpx;
  text-align: center;
}

.product-gallery-dots {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 22rpx;
  z-index: 2;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
}

.product-gallery-dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 999rpx;
  background: rgba(246, 239, 231, 0.24);
}

.product-gallery-dot.is-active {
  width: 28rpx;
  background: var(--gold-strong);
}

.product-summary,
.product-content {
  padding: 26rpx 24rpx;
}

.product-name {
  display: block;
  color: var(--ink);
  font-size: 30rpx;
  font-weight: 600;
  line-height: 1.38;
}

.product-subtitle {
  display: block;
  margin-top: 8rpx;
  color: var(--ink-soft);
  font-size: 18rpx;
  line-height: 1.55;
}

.product-wine-line {
  display: block;
  margin-top: 8rpx;
  color: var(--ink-faint);
  font-size: 17rpx;
  line-height: 1.5;
}

.product-price-row {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 18rpx;
  margin-top: 20rpx;
}

.product-price-stack {
  min-width: 0;
}

.product-price-label {
  display: block;
  color: var(--ink-faint);
  font-size: 16rpx;
  line-height: 1;
}

.product-price-main {
  display: flex;
  align-items: baseline;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-top: 10rpx;
}

.product-price-value {
  color: var(--gold-strong);
  font-size: 40rpx;
  font-weight: 700;
  line-height: 1;
  font-variant-numeric: tabular-nums;
}

.product-price-market {
  color: var(--ink-faint);
  font-size: 18rpx;
  text-decoration: line-through;
  font-variant-numeric: tabular-nums;
}

.product-stock {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44rpx;
  padding: 0 16rpx;
  border-radius: 999rpx;
  background: rgba(142, 164, 113, 0.14);
  color: #b8d093;
  flex-shrink: 0;
}

.product-stock.is-out {
  background: rgba(220, 38, 38, 0.12);
  color: #ef8c8c;
}

.product-stock-text {
  font-size: 16rpx;
  line-height: 1;
}

.product-tag-row {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  margin-top: 18rpx;
}

.product-tag {
  display: inline-flex;
  align-items: center;
  min-height: 38rpx;
  padding: 0 14rpx;
  border-radius: 999rpx;
  background: rgba(244, 235, 225, 0.05);
  color: var(--ink-soft);
  font-size: 15rpx;
  line-height: 1;
}

.summary-divider,
.content-divider {
  height: 1rpx;
  margin: 22rpx 0 20rpx;
  background: rgba(226, 199, 159, 0.1);
}

.sku-section {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.sku-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 20rpx;
}

.sku-title {
  color: var(--ink-soft);
  font-size: 18rpx;
  line-height: 1;
}

.sku-selected {
  color: var(--ink-soft);
  font-size: 17rpx;
  line-height: 1.4;
  text-align: right;
  min-width: 0;
}

.sku-row {
  display: flex;
  align-items: stretch;
  gap: 10rpx;
  width: 100%;
}

.sku-option {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12rpx;
  flex: 1 1 0;
  min-width: 0;
  min-height: 82rpx;
  padding: 0 18rpx;
  border-radius: 18rpx;
  border: 1rpx solid rgba(226, 199, 159, 0.1);
  background: rgba(244, 235, 225, 0.04);
  color: var(--ink);
  transition: background 180ms ease, border-color 180ms ease, opacity 180ms ease;
}

.sku-option.is-active {
  border-color: rgba(231, 200, 150, 0.28);
  background: rgba(231, 200, 150, 0.1);
  box-shadow: 0 10rpx 18rpx rgba(0, 0, 0, 0.1);
}

.sku-option-hover {
  opacity: 0.88;
}

.sku-option-name {
  flex: 1;
  min-width: 0;
  font-size: 18rpx;
  line-height: 1.35;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.sku-option-price {
  flex-shrink: 0;
  color: var(--gold-strong);
  font-size: 18rpx;
  font-weight: 600;
  line-height: 1;
}

.content-block {
  display: flex;
  flex-direction: column;
  gap: 14rpx;
}

.content-title {
  color: var(--ink);
  font-size: 20rpx;
  font-weight: 600;
  line-height: 1;
}

.content-copy {
  color: var(--ink-soft);
  font-size: 18rpx;
  line-height: 1.72;
}

.highlight-list {
  display: flex;
  flex-direction: column;
  gap: 14rpx;
}

.highlight-item {
  display: flex;
  align-items: flex-start;
  gap: 12rpx;
}

.highlight-bullet {
  width: 10rpx;
  height: 10rpx;
  margin-top: 8rpx;
  border-radius: 999rpx;
  background: var(--gold);
  flex-shrink: 0;
}

.highlight-text {
  flex: 1;
  color: var(--ink-soft);
  font-size: 18rpx;
  line-height: 1.62;
}

.meta-list {
  display: flex;
  flex-direction: column;
}

.meta-row {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20rpx;
  padding: 18rpx 0;
  border-bottom: 1rpx solid rgba(226, 199, 159, 0.08);
}

.meta-row:last-child {
  padding-bottom: 0;
  border-bottom: 0;
}

.meta-label {
  flex-shrink: 0;
  color: var(--ink-faint);
  font-size: 17rpx;
  line-height: 1.5;
}

.meta-value {
  text-align: right;
  color: var(--ink);
  font-size: 17rpx;
  line-height: 1.58;
}

.sticky-action {
  position: fixed;
  left: 18rpx;
  right: 18rpx;
  bottom: 18rpx;
  z-index: 24;
  display: flex;
  gap: 12rpx;
  padding: 12rpx;
  border-radius: 28rpx;
  border: 1rpx solid rgba(226, 199, 159, 0.08);
  background:
    linear-gradient(180deg, rgba(20, 15, 14, 0.98), rgba(11, 9, 8, 0.98)),
    rgba(11, 9, 8, 0.98);
  box-shadow: 0 -8rpx 24rpx rgba(0, 0, 0, 0.16);
  backdrop-filter: blur(24rpx);
  -webkit-backdrop-filter: blur(24rpx);
}

.sticky-button {
  flex: 1;
  min-height: 80rpx;
  border-radius: 20rpx;
  font-size: 20rpx;
  line-height: 1;
}

.sticky-button-secondary {
  border: 1rpx solid rgba(214, 177, 124, 0.18);
  background: rgba(244, 235, 225, 0.04);
  color: var(--ink);
}

.sticky-button-secondary-hover {
  background: rgba(244, 235, 225, 0.08);
}

.sticky-button-primary {
  box-shadow: 0 14rpx 28rpx rgba(135, 91, 50, 0.18);
}

.sticky-button-primary-hover {
  opacity: 0.9;
}

.sticky-button[disabled] {
  opacity: 0.4;
  box-shadow: none;
}
